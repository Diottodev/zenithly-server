name: CI/CD Pipeline

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: zenithly_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.12.3

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Create test environment file
        run: |
          echo "DATABASE_URL=postgresql://postgres:postgres@localhost:5432/zenithly_test" > .env
          echo "JWT_SECRET=test_secret_key_for_testing_only" >> .env
          echo "BETTER_AUTH_SECRET=test_better_auth_secret_for_testing" >> .env
          echo "PORT=8080" >> .env
          echo "NODE_ENV=test" >> .env

      - name: Run database migrations
        run: pnpm db:migrate

      - name: Run linting
        run: pnpm lint

      - name: Run tests
        run: pnpm test

  deploy:
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/master' && github.event_name == 'push'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.12.3

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build application
        run: pnpm build || echo "No build script found, skipping build step"

      - name: Deploy to EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            export PATH="$HOME/.local/share/pnpm:$PATH"

            # Ir para diretório da aplicação (ajuste o caminho correto)
            cd /home/ubuntu/zenithly-server || cd /var/www/zenithly-server || exit 1

            # Atualizar código
            git pull origin master

            # Carregar variáveis de ambiente
            export $(grep -v '^#' .env | xargs)

            # Instalar dependências
            pnpm install --frozen-lockfile

            # Rodar migrações
            pnpm db:migrate

            # Reiniciar app no PM2
            pm2 reload ecosystem.config.cjs --env production || pm2 start ecosystem.config.cjs --env production

            # Mostrar status PM2
            pm2 status
